{
  "name": "drafty-server",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -w -p tsconfig.json",
    "dev:local": "nodemon --legacy-watch",
    "dev": "cd scripts && sh start-server-dev.sh",
    "term": "cd scripts && sh start-server-container-terminal.sh",
    "prisma:gen": "npx prisma generate",
    "prisma:seed": "npx prisma db seed"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "4.12.0",
    "fastify": "4.15.0"
  },
  "devDependencies": {
    "@types/node": "^18.15.10",
    "nodemon": "^2.0.20",
    "prisma": "4.12.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2"
  }
}
